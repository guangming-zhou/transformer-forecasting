# 项目说明：ETTh1 多步时间序列预测与模型比较

## 项目定位

这是一个独立的时间序列预测实证项目，不是某篇论文的完整复现。项目围绕一个具体问题展开：

> 在统一、无前视信息的 ETTh1 协议下，基础 Transformer、LSTM、DLinear 与平凡基线的表现和稳定性有何差异？

项目覆盖数据协议、模型训练、多随机种子评测、测试、CI 与推理 Demo。旧版自定义实验完整保留，
但正式主结果使用新增的官方 ETTh1 原始数据协议。

## 技术栈

- Python 3.10+、PyTorch、NumPy、Pandas、Matplotlib；
- Transformer、LSTM、DLinear-S、ARIMA、persistence；
- pytest、GitHub Actions、YAML 配置、串行 sweep；
- FastAPI 与 Streamlit 推理 Demo。

## 数据与协议

### 正式协议

- 数据：上游原始 ETTh1，共 17,420 条小时记录、7 个变量；
- 任务：输入过去 96 小时，预测未来 96 小时 OT；
- 切分：固定 12 个月训练、4 个月验证、4 个月测试；
- 训练/验证/测试目标行数：8640 / 2880 / 2880；
- 窗口数：8449 / 2785 / 2785；
- scaler 只在训练段拟合；
- 不替换重复段或零值段，不进行 σ 裁剪，保证测试目标保持上游原始值；
- 验证和测试只向前借输入上下文，评分目标严格位于各自 split 内。

配置入口：`configs/official_etth1_96.yaml`。

### 历史自定义协议

早期实验采用 60/20/20 时间切分，并对完整序列执行异常标记和双向插值。该实验保留用于
数据审计和方法演进记录，但不再称为官方 LSTF 结果，也不作为新的正式主结论。

## 核心实现

1. **数据管道**：支持固定官方切分、自定义时间切分和季节分层切分；scaler 仅由训练段拟合；
   滑窗不会跨越不连续区间。
2. **模型**：实现 encoder-only Transformer、LSTM 与 DLinear-S，并提供 persistence、ARIMA
   等基线。DLinear-S 的目标输出只依赖 OT 历史，文档不再将它描述成使用全部7个变量。
3. **可复现训练**：固定 Python/NumPy/PyTorch/DataLoader 随机源；checkpoint 同时记录文件
   指纹和权重内容指纹。
4. **安全汇总**：随机模型必须具有固定的5个 seed `{42, 0, 1, 2, 9999}`；缺少任意 seed
   或混入错误 `seq_len/pred_len/split_mode` 的结果时，汇总入口直接拒绝生成结论。
5. **统计口径**：报告 mean ± sample std，并使用相同 seed 的配对差值与95% Student-t
   置信区间。旧版 `2×max(σ)` 只作为历史稳定性启发式，不称为显著性检验。
6. **工程验证**：单元测试、数据/模型/split smoke、API契约测试与双版本 CI；checkpoint
   推理按 batch 执行，避免长窗口全量推理造成内存峰值。

## 正式结果：ETTh1 原始数据 96→96

测试集包含 2785 个预测起点，指标单位为摄氏度，随机模型使用完整5 seed：

| 模型 | 实际输入 | MAE | RMSE | 相对 persistence 的配对差值（95% CI） |
|---|---|---:|---:|---:|
| persistence | OT | 1.8654 | 2.4151 | — |
| **DLinear-S** | **OT** | **1.7346 ± 0.0274** | **2.3482 ± 0.0359** | **−0.1308 [−0.1649, −0.0968]** |
| LSTM | 7变量 | 5.7098 ± 0.4259 | 6.1899 ± 0.4104 | +3.8444 [+3.3155, +4.3732] |
| Encoder-only Transformer | 7变量 | 7.7954 ± 0.5850 | 9.0548 ± 0.7643 | +5.9300 [+5.2036, +6.6563] |

机器可读结果：`outputs/official96/head_to_head.json`。

## 可以支持的结论

1. 在当前官方原始协议和训练配方下，DLinear-S 的整体 MAE 稳定优于 persistence。
2. 当前 LSTM 和 encoder-only Transformer 明显失败，增加模型复杂度没有带来更好结果。
3. 该结果只适用于本项目中的具体实现、数据段与训练配方，不能推广为“所有 Transformer
   都不适合时间序列”。
4. DLinear-S 与多变量模型接收的信息不同，因此表格用于展示实际预测器表现；严格的
   S/MS 架构归因需要分别比较。

## 已知限制

- 正式协议目前只完成 96→96，尚未覆盖 192/336/720；
- 只有 ETTh1 一个数据集；
- 没有加入 PatchTST、iTransformer 等现代架构；
- 配对置信区间反映 seed 波动，尚未加入时间块 bootstrap；
- Transformer seed2 的训练耗时异常，准确率结果保留，但该次耗时不用于效率比较；
- Demo 仍默认使用历史 24→24 checkpoint，正式96→96结果主要作为离线实验产物。

## 验证命令

```bash
python -m pytest -q
python -m utils.smoke_test
python -m models.smoke_test
python -m utils.smoke_split_modes
python -m demo.test_api

python eval/head_to_head.py \
  --config configs/official_etth1_96.yaml \
  --ckpt "Transformer=outputs/official96/transformer/transformer/ckpt" \
  --seed-json "DLinear-S=outputs/official96/dlinear/metrics_dlinear_seed{s}.json" \
  --seed-json "LSTM=outputs/official96/lstm/metrics_lstm_seed{s}.json" \
  --out outputs/official96/head_to_head.json \
  --tag "ETTh1 official raw 96->96"
```
